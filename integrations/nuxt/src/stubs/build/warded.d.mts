// Typecheck-only stub for the `warded.mjs` build template the module
// writes from the app's contract.
import type { AppContract } from "./types/warded";

export declare const contract: AppContract;
